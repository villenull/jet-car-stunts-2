# Session close — 2026-09-17 ~20:20 UTC (paused by user)

## Live Deck truth (final probe, all SSH stopped after)
- Runner `8461` + qemu `8480`, portrait AVD `800x1280`, base
  `app 800 x 1280`, `mCurrentOrientation=1`. Game installed + booting.
- `showCompatibilityWarning=false` present in `Emulator.conf` + `AVD.conf`
  but the RAM dialog (`Suggested minimum system RAM ... 16384 MiB,
  available 14818 MiB`) STILL appeared on this boot — flag disproven.
- Host Qt window renders the game 90° off. Guest framebuffer menus were
  upright in earlier shots; host window never held upright.
- Deck repo @ `f04862a`, clean. All lanes stood down; no reboot/relaunch/
  kill/patch/screenshot in flight.

## What holds (survives any pause)
- Payload: `~/Downloads/com.trueaxis.jetcarstunts2.apk` +
  `deck:~/Projects/JCS2/staging/pristine-v1.0.23.apk` (`779f2103…7c30fe`).
- Signed variant: lib `cb2bd45b…ffb1`, `versionCode 29`, cert `47:B6…:C6:67`,
  install markers + 280M userdata intact. No reinstall needed.
- Deck provisioned: `runtime/` SDK, Go 1.27.1, JDK 21, controller,
  helper JAR, display pins, Steam art staged+synced.
- Repo: offline `466/466 OK`. Single branch `main` after this commit.

## Open (for next agent)
1. Orientation: portrait `800x1280` + console `rotate` still sideways in
   the Qt window. 6 combos tried (landscape-native, no-rotate, skin
   flags, wm-size, rotation lock, portrait+rotate) — none held upright.
   Do not repeat; evidence in this thread's screenshots.
2. RAM warning: `showCompatibilityWarning=false` does NOT suppress it.
3. Desktop fullscreen: XWayland clamps (`~706` wide / `360x640` collapse).
   Prior fullscreen was the GamingMode toolbar-hide path, not Desktop.
4. Never attempted: Steam register, PLAY drive, release upload retry.
