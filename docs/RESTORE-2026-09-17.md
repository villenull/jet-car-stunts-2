# JCS2 restore handoff — 2026-09-17 (Deck fully wiped; start here)

**Status date:** 2026-09-17 ~07:45 UTC. The Steam Deck was fully restored;
**nothing Deck-side survived**. This file is the entry point for a fresh chat.
Prior handoffs (`README.md` 2026-09-16, `docs/CONTINUATION.md`) describe a
world that no longer exists on hardware; their offline source/tooling claims
still hold, every live/personal claim does not.

## 1. Branch / repo pointers (all pushed)

- Local repo: `/home/villenull/Projects/jet-car-stunts-2`, branch `v2`
  tracking `origin/v2`.
- `origin/main` = `fa7665e` (initial publication).
- `origin/v2` = `613797e` + this file (pending push at write time):
  - `acf3c5a` — `packaging/bootstrap_linux_guest.py` (new),
    `packaging/test_bootstrap_linux_guest.py` (new, 14 tests),
    `linux-launcher/tilt_control/test_tilt.py` (mock-target fix + off-Deck skip).
  - `613797e` — `dev-deck-display/` (4 files) + root `README.md` layout line
    + `docs/REPOSITORY-CONTENTS.md` manifest row.
- Offline suite at last run: **463/463 OK across 16 suites**
  (`python3 run-offline-tests.py`; 461 pre-existing + tilt fix + 14 new
  bootstrap tests − counting overlap).

## 2. Measured Deck state after restore (read-only SSH survey, no writes)

- Host `steamdeck`, Omarchy 4.0.4, kernel `7.2.5-3-omarchy`, up 24 min at
  survey; disk **26G used / 449G avail of 475G** (was 120G used pre-wipe).
- `~/Projects/` exists but is **empty**; no `~/Projects/JCS2`, no
  `backups/` / `analysis/` / `staging/` / `dist/`, no `*.apk` anywhere under
  `/home/deck` (maxdepth 4), no `~/Android`, no `/opt/android-sdk`.
- `/dev/kvm` present; `/usr/bin/steam` present (fresh Steam, no JCS2
  shortcut verified).
- Session layer **partial**: `/usr/bin/steamos-session-select` and
  `/usr/local/bin/deck-session-select` exist, but
  `/usr/local/share/wayland-sessions/` holds only `omarchy.desktop` and
  `/usr/share/wayland-sessions/` only `hyprland*.desktop` — **no
  `gamescope-wayland.desktop` found**. Verify before any GamingMode switch.
- Display pins **lost**: `eDP-1` and `DP-1` both enabled (was eDP-1-only).
  Re-apply from `dev-deck-display/README.md` once provisioned.
- SSH was flaky across the wipe window (`Permission denied (publickey)` at
  one point, key auth OK during the survey; host pingable at
  `192.168.100.25`). Do not assume it; re-establish as needed. No SSH
  writes were made after the wipe was discovered.

## 3. What survives (in git, branch `v2`)

- Full prior source/tooling (branch `main` baseline) plus the two `v2`
  commits above.
- `packaging/bootstrap_linux_guest.py`: Linux fresh-guest bootstrap,
  dry-run default, Windows-launcher stages ported (config/identity rc2,
  space+license, port bind-probe rc3 with `SO_REUSEADDR=1`, AVD claim with
  `.jcs2-owned` + adopt-if-never-booted, `adb connect` boot loop,
  `swiftshader_indirect` GPU, `install-multiple -r --no-streaming`,
  `.jcs2-install-state` skip, helper push, detach-after-launch). Usage:
  `python3 packaging/bootstrap_linux_guest.py --help`.
- `dev-deck-display/`: `gamescope-session-edp1` (runtime `-O eDP-1`
  patch wrapper), `override.conf` (user drop-in content),
  `monitors-dp1-disable.snippet.lua` (Hyprland DP-1 disable), `README.md`
  (why: `-O '*,eDP-1'` ranks externals 0 over panel 1 — strict minimum
  wins; apply/verify/revert). Files are **reconstructed from session
  transcripts** (Deck SSH died before a pull-back diff); re-diff against
  the Deck after re-applying.
- Tilt test fixes (stale `_find_motion_event_node` mock → real
  `_discover_motion_node`; Deck-IMU test skips off-Deck).

## 4. What is LOST (not in git, no known copy)

- Authorized 5-split USB set (`backups/usb-20260910T005449Z`, base
  `41043f68cc28e0f82a83a996be24cc6e27dc7b35c0a47fde45c08e2146d6bfa9`,
  self-consistent with its SHA256SUMS) and the divergent dist-tree set
  (base `167b9a26…`, differs at byte 5 — likely patched, never resolved).
- Personal guest (`hardened_api28`, unlock baseline, stable LOW, saves),
  host AVD backups, DB86 signing key material.
- SDK trees, AVD images, helper/controller binaries, Steam VDF/art edits.
- Overnight fresh guest (`staging/jcs2-fresh-avd3`, booted, 5/5 USB splits
  installed, helper pushed, game resumed pid 4514, vCode 29 / v1.0.23) and
  all install evidence.
- This PC also holds **zero** game payload: no `*.apk` under
  `/home/villenull` (maxdepth 5), no local `backups/`/`analysis/`/`dist/`/
  `runtime/`. A Ventoy USB is mounted (`/run/media/villenull/Ventoy`) but
  JCS2 payload presence there was never confirmed.
- Nothing on the latest game build was user-tested before the wipe (user:
  "did not get a chance to test").

## 5. What was PROVEN before the wipe (results, not artifacts)

- Bootstrap end-to-end on Deck hardware: fresh AVD claimed → swiftshader
  boot `Boot completed in ~29 s` → isolation → 5/5 install → markers
  (`.jcs2-install-state`, `.jcs2-bootstrap-state`) → helper push →
  `monkey` launch → game resumed. Script fixes from the trial are all in
  the committed file (GPU, `adb connect`, TIME_WAIT probe, adopt path,
  detach-after-launch).
- GamingMode pin: `selecting connector eDP-1`, `800x1280@90Hz`, Steam
  gamepadui up, touch on panel — then wiped with the Deck.
- SSH survives GamingMode switches (10/10 probes, zero gaps); mechanism:
  sshd is a system service.

## 6. Next steps for the new chat (in order)

1. **Payload question first** (user was asked; answers: Deck "fully wiped";
   rebuild scope: "monitor setup should be reinstalled"). If no USB backup
   or other copy of the authorized APK set / saves / DB86 key exists, the
   game **cannot be reinstalled** — re-sourcing authorized payload is step
   zero, and no bootstrap trial should run until it lands.
2. Re-provision Deck: clone branch `v2`, restore `steamos-session-select`
   GamingMode target if still missing, re-apply `dev-deck-display/`,
   fetch SDK per `packaging/installer/runtime-lock.json`, rebuild
   controller (`go build`) + helper JAR (needs Android SDK `javac`+`d8`;
   no in-repo recipe — known gap).
3. Fresh-guest bootstrap trial with the recovered splits; verify
   `pm path` 5/5 + cert + versionCode gates.
4. Redo all live gates from scratch: TiltDrive card, GamingMode quit,
   Steam art pickup, audible/PLAY smoke, editor/maps isolated slots.

## 7. Standing notes

- Overnight authority grant ("truly anything", incl. personal
  guest/backups/keys) is moot — those objects no longer exist. New chat
  should re-confirm authority before any destructive step.
- User asked that Deck-only display be reinstalled; procedure is
  `dev-deck-display/README.md`.
- No publishing was authorized (standing); Deck morning-state preference
  (GamingMode) is moot post-wipe.
